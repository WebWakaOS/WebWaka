{
  "name": "@webwaka/ussd-gateway",
  "version": "0.1.0",
  "description": "[Pillar 1] USSD gateway Worker \u2014 *384# shortcode via Africa's Talking",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env staging",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.14",
    "@webwaka/types": "workspace:*",
    "@webwaka/auth": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0",
    "wrangler": "^4.0.0"
  }
}
