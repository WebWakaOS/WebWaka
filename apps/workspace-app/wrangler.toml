# WebWaka Workspace App — Cloudflare Pages
# Deploy via: wrangler pages deploy dist --project-name webwaka-workspace-app

name = "webwaka-workspace-app"
pages_build_output_dir = "dist"

[env.preview]
pages_build_output_dir = "dist"

[env.production]
pages_build_output_dir = "dist"
