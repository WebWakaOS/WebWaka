# WebWaka Marketing Site — Cloudflare Pages
# Deploy via: wrangler pages deploy dist --project-name webwaka-marketing-site

name = "webwaka-marketing-site"
pages_build_output_dir = "dist"

[env.staging]
pages_build_output_dir = "dist"

[env.production]
pages_build_output_dir = "dist"
