# Platform Admin SPA — Environment Variables
# Copy to .env.local for local development

# Required: WebWaka API URL
VITE_API_URL=https://api-staging.webwaka.com

# Optional: Proxy target for local development (used by vite.config.ts proxy)
VITE_API_PROXY_TARGET=http://localhost:8787
