{
  "name": "@webwaka/public-discovery",
  "version": "0.1.0",
  "private": true,
  "description": "[Pillar 3] Public discovery Worker — geography-first listing and multi-vendor marketplace frontend.",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "@webwaka/types": "workspace:*",
    "@webwaka/entities": "workspace:*",
    "@webwaka/i18n": "workspace:*",
    "@webwaka/search-indexing": "workspace:*",
    "@webwaka/white-label-theming": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "^3.90.0"
  }
}
