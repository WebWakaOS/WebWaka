# Replit Agent 4 — Milestone 3 Implementation Prompt

**Repo:** https://github.com/WebWakaDOS/webwaka-os  
**Status:** Milestone 3 ACTIVE — Base44 verified M2 complete  
**Branch:** Create `feat/milestone-3` from current `main`

## 🎯 EXACT DELIVERABLES (7 items only)

### 1. `packages/entities` — CRUD layer
```
src/ids.ts — branded ID generators (ind_, org_, plc_, off_, prf_, wsp_, brs_)
src/repository/individuals.ts — tenant-aware CRUD
src/repository/organizations.ts — tenant-aware CRUD  
src/repository/workspaces.ts — auto-membership on create
src/repository/places.ts — geography ancestry computation
src/repository/profiles.ts — claim state transitions
```

### 2. `packages/entitlements` — plan engine
```
5 plans: free/starter/growth/professional/enterprise
src/evaluate.ts — layer/user/place limit checks
src/guards.ts — requireLayerAccess(), requireBrandingRights()
```

### 3. `packages/relationships` — typed links
```
15 kinds from relationship-schema.md
D1 migration 0007: relationships table + indexes
src/repository.ts — create/list/delete (tenant filtered)
```

### 4. `apps/api` — Hono Worker
```
10 endpoints exactly:
/health, /auth/me, /auth/verify
/geography/places/:id, /:id/children, /:id/ancestry  
/entities/individuals (POST/GET), /organizations (POST/GET)
JWT middleware → @webwaka/auth
```

### 5. `packages/offline-sync` — scaffold only
```
SyncQueueItem, SyncStatus, SyncAdapter interface
No runtime implementation
```

### 6. `packages/ai-abstraction` — scaffold only
```
AIProvider, AIRequest, AIResponse, AIAdapter interface  
No provider SDK calls
```

### 7. M2 fixes
```
#8: LGA(774) + ward(8,814) seeds
#11: geography buildIndexFromD1()
#12: CandidateRecord.id + political_assignments UNIQUE
```

## 🚫 STRICT BOUNDARIES
```
❌ NO vertical features (commerce/transport/civic)
❌ NO Express/Node servers  
❌ NO direct OpenAI/etc calls
❌ NO frontend apps
❌ NO payment integration
❌ NO main pushes — PR only
```

## 📋 PROCESS
```
1. git checkout main && git pull
2. git checkout -b feat/milestone-3
3. Implement 7 deliverables above
4. pnpm install && pnpm run typecheck && pnpm run test
5. git push && gh pr create --title "feat(milestone-3): vertical scaffolding + API"
6. Label PR: milestone-3, review-needed, base44
```

## ✅ SUCCESS = CI GREEN + 7 deliverables