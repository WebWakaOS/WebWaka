{
  "name": "@webwaka/core",
  "version": "0.1.0",
  "private": true,
  "description": "[Infra] Core utilities: circuit breaker, KV safe-get, and shared runtime helpers for WebWaka OS Workers.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./circuit-breaker": "./src/circuit-breaker.ts",
    "./kv-safe": "./src/kv-safe.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo 'no build step — Workers consume TypeScript source directly'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  }
}
