{
  "name": "@webwaka/workflows",
  "version": "0.1.0",
  "description": "[Phase 2] Workflow Engine MVP — WorkflowDefinition, WorkflowInstance, payout-approval + case-resolution workflows",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
