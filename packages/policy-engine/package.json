{
  "name": "@webwaka/policy-engine",
  "version": "0.1.0",
  "description": "[Cross-cutting] Platform policy rule engine — INEC caps, NDPR gates, AI gates, content moderation gates, payout approval rules",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@webwaka/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.1.9"
  },
  "keywords": [
    "policy",
    "rules",
    "compliance",
    "inec",
    "ndpr",
    "nigeria"
  ]
}
