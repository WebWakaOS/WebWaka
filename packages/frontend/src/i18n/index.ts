/**
 * i18n module — Naija Pidgin + English locale strings (M7e)
 * (docs/governance/platform-invariants.md — P2 Nigeria First)
 *
 * Locale strings are plain TypeScript objects for strict-mode compatibility.
 * Runtime i18next framework integration is M8 frontend work.
 */
export { pcmLocale } from './pcm.js';
export { enLocale } from './en.js';
export type { LocaleKey } from './types.js';
